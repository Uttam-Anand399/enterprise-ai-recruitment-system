import AuthCard from '../components/AuthCard.jsx';

function Register() {
  return (
    <AuthCard
      mode="register"
      title="Create an account for the hiring platform."
      subtitle="Register page skeleton with role selection, responsive form controls, and production-ready spacing."
    />
  );
}

export default Register;
