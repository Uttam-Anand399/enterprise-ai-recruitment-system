import AuthCard from '../components/AuthCard.jsx';

function Login() {
  return (
    <AuthCard
      mode="login"
      title="Login to your recruitment workspace."
      subtitle="Access candidate, recruiter, or admin dashboards through a polished static authentication screen."
    />
  );
}

export default Login;
